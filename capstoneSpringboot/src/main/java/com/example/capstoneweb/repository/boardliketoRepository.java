package com.example.capstoneweb.repository;

import com.example.capstoneweb.model.boardliketo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface boardliketoRepository extends JpaRepository<boardliketo, Integer> {
}
